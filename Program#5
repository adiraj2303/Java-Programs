Given a String and an array of prefixes. The task is to check whether the
given String starts with any of the given prefixes or not.

import java.util.*;
class PrefixSearch { 

    public static void main(String[] args)  {
   
 String[] arr = { "Gurbani", "Dheeraj", "Adiraj" };                     
 Scanner sc =new Scanner(System.in);
 System.out.println("Enter the string that you want check : ");
 String str = sc.next();
   int flag =0; 
        for (int i = 0; i < 3; i++) { 
              if (str.startsWith(arr[i])) { 
              flag= 1; 
              break; 
            } 
     } 
    if (flag ==1) {

        System.out.println("Yes, the given string starts with one of the prefixes."); 
}
    else{

        System.out.println("No, the given string do not starts with one of the prefixes."); 
       }
    } 
} 

